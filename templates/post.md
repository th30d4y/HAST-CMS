---
# REQUIRED — the title of the article
title: "Your Article Title Here"

# OPTIONAL — override the URL slug (defaults to the filename)
# slug: "custom-url-slug"

# REQUIRED — short description for search engines and article indexes (max ~160 chars)
description: "Short description of this article displayed in indexes and search results."

# REQUIRED — publication date (ISO 8601: YYYY-MM-DD)
date: 2026-08-03

# OPTIONAL — date of last substantive update (ISO 8601: YYYY-MM-DD)
# updated: 2026-08-03

# OPTIONAL — author name (must match an entry in src/content/authors/)
author: "HAST CMS"

# OPTIONAL — category for this article (creates /category/<slug>/ automatically)
category: "Security Research"

# OPTIONAL — list of tags (creates /tag/<slug>/ pages automatically)
tags:
  - example-tag
  - another-tag

# OPTIONAL — hashtags (creates /hashtag/<normalized>/ pages automatically)
# Include the # prefix or omit it — both work
hashtags:
  - "#CyberSecurity"
  - "#SecurityResearch"

# OPTIONAL — show this article in the Featured section on the homepage
featured: false

# OPTIONAL — pin this article to the top of the homepage
pinned: false

# REQUIRED — set true to hide from all public indexes until you remove this line
draft: false

# OPTIONAL — part of a multi-part series
# series: "Series Name Here"
# seriesOrder: 1

# OPTIONAL — path to cover image (relative to /public/ or absolute URL)
# cover: "/images/my-cover.jpg"

# OPTIONAL — custom Open Graph image (defaults to cover or site default)
# ogImage: "/images/og-my-article.jpg"

# OPTIONAL — canonical URL if republished from another source
# canonical: "https://original-source.com/article"

# OPTIONAL — show table of contents sidebar (requires H2/H3 headings)
toc: true

# OPTIONAL — allow comments (no-op until a comment provider is configured)
comments: false

# OPTIONAL — exclude from search engines and sitemap
noindex: false
---

Your article content starts here.

Write in standard Markdown — headings, lists, code blocks, tables, and all GFM features are supported.
